# OpenAPI 平台 - 数据库设计

## 一、数据库概览

使用 SQLite 数据库，包含以下核心表：

| 表名 | 说明 |
|------|------|
| users | 用户表 |
| apps | 应用表 |
| apis | 接口定义表 |
| api_access | 接口权限关联表 |
| access_tokens | 访问令牌表 |
| api_logs | 接口调用日志表 |

## 二、实体关系图

```
┌──────────────┐       ┌──────────────┐
│    users     │       │     apis     │
├──────────────┤       ├──────────────┤
│ id (PK)      │       │ id (PK)      │
│ username     │       │ name         │
│ email        │       │ path         │
│ password_hash│       │ method       │
│ created_at   │       │ description  │
│ updated_at   │       │ status       │
└──────┬───────┘       │ user_id (FK) │
       │               │ created_at   │
       │               │ updated_at   │
       │               └──────┬───────┘
       │                      │
       │               ┌──────┴───────┐
       │               │ api_access   │
       │               ├──────────────┤
       │               │ id (PK)      │
       │               │ api_id (FK)  │
       │               │ app_id (FK)  │
       │               │ created_at   │
       │               └──────┬───────┘
       │                      │
┌──────┴───────┐       ┌──────┴───────┐
│    apps      │       │ access_tokens│
├──────────────┤       ├──────────────┤
│ id (PK)      │       │ id (PK)      │
│ name         │       │ token        │
│ app_id       │       │ token_type   │
│ app_secret   │       │ expires_at   │
│ description  │       │ app_id (FK)  │
│ user_id (FK) │       │ user_id (FK) │
│ created_at   │       │ created_at   │
│ updated_at   │       └───────────────┘
└──────────────┘               │
                              │
              ┌───────────────┴───────────────┐
              │         api_logs             │
              ├──────────────────────────────┤
              │ id (PK)                       │
              │ api_id (FK)                   │
              │ app_id (FK)                   │
              │ method                        │
              │ path                          │
              │ query_params                  │
              │ request_body                  │
              │ response_status                │
              │ response_body                  │
              │ ip_address                     │
              │ user_agent                     │
              │ latency_ms                     │
              │ created_at                     │
              └──────────────────────────────┘
```

## 三、详细表结构

### 3.1 users（用户表）

```sql
CREATE TABLE users (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    username VARCHAR(50) UNIQUE NOT NULL,
    email VARCHAR(100) UNIQUE NOT NULL,
    password_hash VARCHAR(255) NOT NULL,
    is_active BOOLEAN DEFAULT 1,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP
);

CREATE INDEX idx_users_username ON users(username);
CREATE INDEX idx_users_email ON users(email);
```

**说明**：
- `username`: 用户名，唯一
- `email`: 邮箱，唯一
- `password_hash`: bcrypt 加密后的密码
- `is_active`: 账号是否启用

---

### 3.2 apps（应用表）

```sql
CREATE TABLE apps (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name VARCHAR(100) NOT NULL,
    app_id VARCHAR(32) UNIQUE NOT NULL,
    app_secret_hash VARCHAR(255) NOT NULL,
    description TEXT,
    user_id INTEGER NOT NULL,
    is_active BOOLEAN DEFAULT 1,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE
);

CREATE INDEX idx_apps_app_id ON apps(app_id);
CREATE INDEX idx_apps_user_id ON apps(user_id);
```

**说明**：
- `app_id`: 应用唯一标识，32位随机字符串
- `app_secret_hash`: 应用密钥哈希（首次创建时显示明文，后续只存哈希）
- `user_id`: 所属用户

---

### 3.3 apis（接口定义表）

```sql
CREATE TABLE apis (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name VARCHAR(100) NOT NULL,
    path VARCHAR(255) NOT NULL,
    method VARCHAR(10) NOT NULL,
    description TEXT,
    request_params TEXT,  -- JSON格式存储参数定义
    request_body TEXT,   -- JSON格式存储请求体
    response_body TEXT,  -- JSON格式存储响应体
    backend_url VARCHAR(255) NOT NULL,  -- 后端实际服务地址
    status VARCHAR(20) DEFAULT 'draft',  -- draft, published, deprecated
    user_id INTEGER NOT NULL,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE
);

CREATE INDEX idx_apis_path ON apis(path);
CREATE INDEX idx_apis_status ON apis(status);
CREATE INDEX idx_apis_user_id ON apis(user_id);
```

**说明**：
- `path`: 接口路径，如 `/api/users`
- `method`: HTTP 方法 GET/POST/PUT/DELETE
- `request_params`: 接口参数定义（JSON）
- `backend_url`: 实际后端服务地址
- `status`: 接口状态 - draft(草稿)、published(已发布)、deprecated(已废弃)

---

### 3.4 api_access（接口权限表）

```sql
CREATE TABLE api_access (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    api_id INTEGER NOT NULL,
    app_id INTEGER NOT NULL,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (api_id) REFERENCES apis(id) ON DELETE CASCADE,
    FOREIGN KEY (app_id) REFERENCES apps(id) ON DELETE CASCADE,
    UNIQUE(api_id, app_id)
);

CREATE INDEX idx_api_access_api_id ON api_access(api_id);
CREATE INDEX idx_api_access_app_id ON api_access(app_id);
```

**说明**：
- 关联应用和接口，表示该应用有权限访问该接口

---

### 3.5 access_tokens（访问令牌表）

```sql
CREATE TABLE access_tokens (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    token VARCHAR(255) UNIQUE NOT NULL,
    token_type VARCHAR(20) NOT NULL,  -- bearer, jwt
    expires_at DATETIME NOT NULL,
    app_id INTEGER NOT NULL,
    user_id INTEGER,  -- OAuth授权时关联的用户
    scope TEXT,  -- 权限范围，JSON格式
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (app_id) REFERENCES apps(id) ON DELETE CASCADE,
    FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE SET NULL
);

CREATE INDEX idx_access_tokens_token ON access_tokens(token);
CREATE INDEX idx_access_tokens_app_id ON access_tokens(app_id);
CREATE INDEX idx_access_tokens_expires ON access_tokens(expires_at);
```

**说明**：
- `token`: 令牌值
- `token_type`: 令牌类型 - bearer(OAuth2.0)、jwt
- `expires_at`: 过期时间
- `scope`: OAuth2.0 授权范围

---

### 3.6 api_logs（接口调用日志表）

```sql
CREATE TABLE api_logs (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    api_id INTEGER NOT NULL,
    app_id INTEGER,
    method VARCHAR(10) NOT NULL,
    path VARCHAR(255) NOT NULL,
    query_params TEXT,
    request_body TEXT,
    response_status INTEGER,
    response_body TEXT,
    error_message TEXT,
    ip_address VARCHAR(45),
    user_agent TEXT,
    latency_ms INTEGER,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (api_id) REFERENCES apis(id) ON DELETE CASCADE,
    FOREIGN KEY (app_id) REFERENCES apps(id) ON DELETE SET NULL
);

CREATE INDEX idx_api_logs_api_id ON api_logs(api_id);
CREATE INDEX idx_api_logs_app_id ON api_logs(app_id);
CREATE INDEX idx_api_logs_created_at ON api_logs(created_at);
```

**说明**：
- 记录每次接口调用的详细信息，用于调试和监控

---

## 四、SQLAlchemy 模型定义

```python
# models.py

from sqlalchemy import Column, Integer, String, Boolean, DateTime, Text, ForeignKey, JSON, UniqueConstraint
from sqlalchemy.orm import relationship, declarative_base
from datetime import datetime

Base = declarative_base()

class User(Base):
    __tablename__ = "users"
    
    id = Column(Integer, primary_key=True, autoincrement=True)
    username = Column(String(50), unique=True, nullable=False)
    email = Column(String(100), unique=True, nullable=False)
    password_hash = Column(String(255), nullable=False)
    is_active = Column(Boolean, default=True)
    created_at = Column(DateTime, default=datetime.utcnow)
    updated_at = Column(DateTime, default=datetime.utcnow, onupdate=datetime.utcnow)
    
    apps = relationship("App", back_populates="user")
    apis = relationship("Api", back_populates="user")

class App(Base):
    __tablename__ = "apps"
    
    id = Column(Integer, primary_key=True, autoincrement=True)
    name = Column(String(100), nullable=False)
    app_id = Column(String(32), unique=True, nullable=False)
    app_secret_hash = Column(String(255), nullable=False)
    description = Column(Text)
    user_id = Column(Integer, ForeignKey("users.id", ondelete="CASCADE"), nullable=False)
    is_active = Column(Boolean, default=True)
    created_at = Column(DateTime, default=datetime.utcnow)
    updated_at = Column(DateTime, default=datetime.utcnow, onupdate=datetime.utcnow)
    
    user = relationship("User", back_populates="apps")
    api_access = relationship("ApiAccess", back_populates="app")
    tokens = relationship("AccessToken", back_populates="app")

class Api(Base):
    __tablename__ = "apis"
    
    id = Column(Integer, primary_key=True, autoincrement=True)
    name = Column(String(100), nullable=False)
    path = Column(String(255), nullable=False)
    method = Column(String(10), nullable=False)
    description = Column(Text)
    request_params = Column(JSON)
    request_body = Column(JSON)
    response_body = Column(JSON)
    backend_url = Column(String(255), nullable=False)
    status = Column(String(20), default="draft")  # draft, published, deprecated
    user_id = Column(Integer, ForeignKey("users.id", ondelete="CASCADE"), nullable=False)
    created_at = Column(DateTime, default=datetime.utcnow)
    updated_at = Column(DateTime, default=datetime.utcnow, onupdate=datetime.utcnow)
    
    user = relationship("User", back_populates="apis")
    access = relationship("ApiAccess", back_populates="api")
    logs = relationship("ApiLog", back_populates="api")

class ApiAccess(Base):
    __tablename__ = "api_access"
    
    id = Column(Integer, primary_key=True, autoincrement=True)
    api_id = Column(Integer, ForeignKey("apis.id", ondelete="CASCADE"), nullable=False)
    app_id = Column(Integer, ForeignKey("apps.id", ondelete="CASCADE"), nullable=False)
    created_at = Column(DateTime, default=datetime.utcnow)
    
    __table_args__ = (UniqueConstraint("api_id", "app_id", name="uq_api_app"),)
    
    api = relationship("Api", back_populates="access")
    app = relationship("App", back_populates="api_access")

class AccessToken(Base):
    __tablename__ = "access_tokens"
    
    id = Column(Integer, primary_key=True, autoincrement=True)
    token = Column(String(255), unique=True, nullable=False)
    token_type = Column(String(20), nullable=False)  # bearer, jwt
    expires_at = Column(DateTime, nullable=False)
    app_id = Column(Integer, ForeignKey("apps.id", ondelete="CASCADE"), nullable=False)
    user_id = Column(Integer, ForeignKey("users.id", ondelete="SET NULL"), nullable=True)
    scope = Column(JSON)
    created_at = Column(DateTime, default=datetime.utcnow)
    
    app = relationship("App", back_populates="tokens")

class ApiLog(Base):
    __tablename__ = "api_logs"
    
    id = Column(Integer, primary_key=True, autoincrement=True)
    api_id = Column(Integer, ForeignKey("apis.id", ondelete="CASCADE"), nullable=False)
    app_id = Column(Integer, ForeignKey("apps.id", ondelete="SET NULL"), nullable=True)
    method = Column(String(10), nullable=False)
    path = Column(String(255), nullable=False)
    query_params = Column(Text)
    request_body = Column(Text)
    response_status = Column(Integer)
    response_body = Column(Text)
    error_message = Column(Text)
    ip_address = Column(String(45))
    user_agent = Column(Text)
    latency_ms = Column(Integer)
    created_at = Column(DateTime, default=datetime.utcnow)
    
    api = relationship("Api", back_populates="logs")
```

---

## 五、初始化数据

项目启动时自动创建数据库表，无需手动执行 SQL。
